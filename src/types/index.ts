// This file contains all the types used in the application.

// Debug state keys
export type DebugKey = "tab" | "mic" | "stt" | "ai";

// Debug state values
export type DebugState = "default" | "off" | "on" | "active" | "warn" | "error";

// // User information
// // Set like this:  const userInfo = { userId, userEmail, userMetadata, orgId, orgName, orgSlug };
// export type UserInfo = {
//   userId: string;
//   userEmail: string;
//   userMetadata: any;
//   orgId?: string;
//   orgName?: string;
//   orgSlug?: string;
// };
